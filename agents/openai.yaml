interface:
  display_name: "Slide90"
  short_description: "Generate a usable 80-point executive deck without formatting work"
  default_prompt: "Use $build-executive-report-slides to preserve my reporting structure and quickly generate an editable 80-point executive deck; ask only if the structure is genuinely unclear."
policy:
  allow_implicit_invocation: true
